fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
SQLAlchemy>=2.0.30
aiosqlite>=0.20
asyncpg>=0.29
alembic>=1.13
redis>=5.0
pandas>=2.2
reportlab>=4.2
# GenAI layer. Installed from the sibling ../genai package (see README).
# Pulls in mistralai + python-dotenv. The adapter in app/integrations/genai.py
# falls back to a deterministic copilot if this is not installed.
mistralai>=0.4,<1.0
python-dotenv>=1.0
# dev / test
pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27
